export default function getHomeworldDetails(homeworld) {
    // TODO:
    // Returnera en HTML-sträng som visar:
    // - name, rotation_period, orbital_period, diameter, climate, gravity, terrain
    // Exempel: <h3>{name}</h3> och <p>Rotation period: {rotation_period}h</p>
}