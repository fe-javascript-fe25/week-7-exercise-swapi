export default function getCharacterDetails(character) {
    // TODO:
    // Returnera en HTML-sträng som visar:
    // - name, height, mass, hair_color, skin_color, eye_color, birth_year, gender
    // Exempel: <h3>{name}</h3> och <p>Height: {height} cm</p>
}